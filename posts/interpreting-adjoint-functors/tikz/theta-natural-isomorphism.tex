%! TeX program = xelatex
\documentclass[crop]{standalone}
\usepackage{amsmath,amssymb,mathrsfs}
\usepackage{quiver}
\usepackage{tikz}
\usepackage{tikz-cd}
\usetikzlibrary{calc,babel}

\usepackage{fontspec} % optional

\begin{document}

\begin{tikzcd}
	{X'} && {F(X')} \\
	\\
	X && {F(X)} && Y \\
	\\
	{X_0} && {F(X_0)}
	\arrow["f", from=1-1, to=3-1]
	\arrow["{f^* \theta_{X}(\theta)}"', curve={height=24pt}, from=1-1, to=5-1]
	\arrow["{F(f)}"', from=1-3, to=3-3]
	\arrow["{f^* \phi}", from=1-3, to=3-5]
	\arrow["{\theta_X(\phi)}", from=3-1, to=5-1]
	\arrow["\phi"{description}, from=3-3, to=3-5]
	\arrow["{F(\theta_X(\phi))}"', from=3-3, to=5-3]
	\arrow["{\phi_0}"', from=5-3, to=3-5]
\end{tikzcd}.

\end{document}
